apply from: "../standalone.gradle" // Uncomment to use module in another project

ext {
    BASENAME = 'instantsearch-android-core'
}

dependencies {
    api libraries.eventbus
    api libraries.algolia

//    implementation libraries.appcompat TODO: Remove once confirmed androidX lib works with v7 app
//    implementation libraries.support
    implementation libraries.appcompatX
    implementation libraries.material

    implementation(libraries.json_path) {
        exclude module: 'asm'
    }
}

apply from: '../release.gradle'